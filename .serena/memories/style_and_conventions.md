Python 3.12+ PyQt6 project. Existing snapshot contracts use `@dataclass(frozen=True, slots=True)`, explicit type annotations, tuple-return helper patterns, and small private extractor helpers prefixed with `_`. Preserve helper conventions such as `_numeric_value`, `_sequence`, `getattr(source, ...)` fallbacks. Avoid `# type: ignore` unless justified. User-visible PyQt Widget strings normally use `QApplication.translate`, QML strings use `qsTr`. Do not modify generated files unless explicitly regenerating derived artifacts.