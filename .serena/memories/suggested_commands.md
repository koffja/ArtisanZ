From repo root, run preflight before edits: `GIT_MASTER=1 git branch --show-current`, `GIT_MASTER=1 git status --short`, `GIT_MASTER=1 git remote -v`. Work commands generally run from `src/`: `python3 -m venv .venv && source .venv/bin/activate`, `pip install -r requirements.txt`, `pip install -r requirements-dev.txt`, `python3 artisan.py`. Focused charge-target verification: `.venv/bin/python -m py_compile artisanlib/charge_manager.py artisanlib/charge_dialog.py artisanlib/main.py artisanlib/canvas.py`; `.venv/bin/python -m pytest test/unitary/artisanlib/test_charge_manager.py -q`. General checks: `.venv/bin/python -m pytest`, `.venv/bin/python -m ruff check .`, `.venv/bin/python -m mypy`, `.venv/bin/python -m pyright`, `.venv/bin/python -m pylint artisanlib plus`, `.venv/bin/python -m codespell`, `pre-commit run --all-files`.