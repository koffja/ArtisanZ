Before completion: run LSP diagnostics on changed files when possible, compile changed Python files with `py_compile`, run the smallest relevant pytest target, then broader suite if requested/applicable, and run `ruff check` on changed Python files. For git commits, use `GIT_MASTER=1` prefix on every git command, inspect status/diff/log before committing, stage only intended files, never push unless explicitly requested, and preserve unrelated untracked/uncommitted user files.